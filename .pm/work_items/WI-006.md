# WI-006 - Real Pose Backend Readiness

Goal: prepare the external worker for RTMPose/MMPose without installing heavy dependencies yet.

Why this next:
- Current MVP proves the cached JSON path.
- The plan points to external inference for v1.
- Real video pixels are the next meaningful gap.

Tasks:
1. `WI-006-A` - backend interface cleanup.
2. `WI-006-B` - RTMPose/MMPose missing-dependency adapter.
3. `WI-006-C` - real inference setup doc and blocker list.

Acceptance:
- Worker has a clean backend boundary.
- `--backend rtmpose` fails clearly if dependencies are missing.
- Setup doc explains exact install/evaluation path.
- No large dependencies are installed.

Out of scope:
- Installing torch/MMPose/OpenCV.
- Running real inference.
- Editing Blender source.
