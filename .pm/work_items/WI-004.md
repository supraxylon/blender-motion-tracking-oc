# WI-004 - External Worker Foundation

Goal: start the external inference side without breaking the repo.

Input:
- `test_data/green_field.mp4`
- `schemas/tracking_result.schema.json`
- `tools/validate_tracking_json.py`
- `examples/sample_tracking_result.json`

Tasks:
1. `WI-004-A` - environment and video probe.
2. `WI-004-B` - worker CLI with fixture backend.
3. `WI-004-C` - validation docs and PM report.

Acceptance:
- We know whether local MMPose install/inference is currently feasible.
- Worker CLI can emit schema-compatible JSON.
- Generated worker output validates.
- The report clearly separates working, broken, and blocked items.

Out of scope:
- Heavy ML install unless explicitly approved.
- Blender source edits.
- In-Blender validation until Blender runtime launches.

