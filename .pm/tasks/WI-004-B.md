# WI-004-B - Worker CLI Scaffold

You are OpenCode. Build this branch only.

Goal: create a worker CLI that can emit cached tracking JSON now, using a fixture backend.

Touch only:
- `tracking_worker/`
- `tests/test_tracking_worker_fixture.py`

Deliver:
- `tracking_worker/__init__.py`
- `tracking_worker/cli.py`
- `tracking_worker/fixture_backend.py`
- `tracking_worker/schema_io.py`
- `tests/test_tracking_worker_fixture.py`

Requirements:
- CLI shape:
  `python -m tracking_worker.cli --input test_data/green_field.mp4 --output outputs/green_field_tracking.json --backend fixture`
- Fixture backend may reuse deterministic sample-like COCO-17 keypoints.
- Output must match `schemas/tracking_result.schema.json`.
- Use stdlib only.
- Create output directories as needed.
- Print concise progress.

Acceptance:
- Worker command writes output JSON.
- `tools/validate_tracking_json.py outputs/green_field_tracking.json` passes.
- Unit/smoke test passes with available Python, or documents Python blocker.

Out of scope:
- Real MMPose inference.
- Package installs.
- Blender source edits.

