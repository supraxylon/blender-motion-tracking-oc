# WI-003-A - JSON Contract

You are OpenCode. Build this branch only.

Goal: define the cached pose JSON contract.

Touch only:
- `schemas/`
- `examples/`
- `tools/validate_tracking_json.py`

Deliver:
- `schemas/tracking_result.schema.json`
- `examples/sample_tracking_result.json`
- `tools/validate_tracking_json.py`

Requirements:
- Schema covers video metadata, frames, persons, bbox, track_id, and COCO 17 keypoints.
- Coordinates are normalized 0..1.
- Scores are 0..1.
- Sample has at least 2 frames, 1 person, 17 keypoints.
- Validator uses stdlib only.
- Validator prints clear pass/fail.

Accept:
- `python tools/validate_tracking_json.py examples/sample_tracking_result.json` passes.
- Bad keypoint count fails.
- No heavy dependencies.

Out:
- No MMPose install.
- No Blender code.
- No retargeting.

Report:
- Files changed
- Commands run
- Validation
- Blockers

