# WI-003-B - Blender Importer

You are OpenCode. Build this branch only.

Goal: create the Blender add-on feature that imports cached pose JSON and creates empties.

Touch only:
- `src/blender_pose_tracker/`

Reference only:
- `blender/`

Deliver:
- `src/blender_pose_tracker/__init__.py`
- `src/blender_pose_tracker/importer.py`

Requirements:
- Blender add-on metadata.
- Panel in the 3D View sidebar.
- File path property for cached JSON.
- Operator to import JSON.
- Create one empty per person/keypoint.
- Name empties predictably: `PoseTrack_<track_id>_<keypoint>`.
- Insert location keyframes by frame.
- Use normalized x/y mapped to simple Blender coordinates.
- Keep code importable enough for `py_compile`.
- If cheap, note the local Blender executable path for later headless testing.

Accept:
- `python -m py_compile src/blender_pose_tracker/__init__.py src/blender_pose_tracker/importer.py` passes, or the same command passes with Blender's bundled Python if `python` is not on PATH.
- No MMPose install.
- No schema edits.
- Do not edit `blender/`.

Out:
- No armatures.
- No smoothing.
- No subprocess worker.
- No UI polish beyond the import path and button.

Report:
- Files changed
- Commands run
- Validation
- Blockers
