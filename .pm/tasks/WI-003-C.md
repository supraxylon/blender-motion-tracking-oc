# WI-003-C - Smoke Checks and Docs

You are OpenCode. Build this branch only.

Goal: make the first feature understandable and checkable.

Touch only:
- `docs/`
- `tests/`
- `README.md`

Reference only:
- `blender/`

Deliver:
- `docs/cached_pose_import.md`
- `tests/smoke_validate_cached_pose.py`
- README update linking the first feature docs.

Requirements:
- Smoke check uses stdlib only.
- Smoke check validates expected files exist.
- Smoke check runs validator on sample JSON if present.
- Smoke check compiles add-on files if present.
- Docs explain: schema -> sample JSON -> Blender import.
- Mention the local Blender 5.1 path if found.
- Keep README change tiny.

Accept:
- `python tests/smoke_validate_cached_pose.py` passes after branches A and B exist, or the same script passes with Blender's bundled Python if `python` is not on PATH.
- Docs are short and practical.

Out:
- No product code.
- No MMPose install.
- No retargeting.
- Do not edit `blender/`.

Report:
- Files changed
- Commands run
- Validation
- Blockers
