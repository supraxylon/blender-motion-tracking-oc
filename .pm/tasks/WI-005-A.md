# WI-005-A - Cached Pose SVG Preview

Objective: create a stdlib-only preview tool for cached tracking JSON.

Context:
- Blender does not launch yet.
- We still need something visual to evaluate the JSON pipeline.

Deliverables:
- `tools/render_tracking_preview.py`
- `outputs/green_field_tracking_preview.svg`

Requirements:
- CLI: `--input <json>` and `--output <svg>`.
- Read `outputs/green_field_tracking.json`.
- Draw the frame box, COCO-17 points, simple skeleton lines, and track IDs.
- Use normalized coordinates from the JSON.
- Use only Python stdlib.

Acceptance:
- This passes:
  `.\blender\blender-5.1.1-windows-x64\blender-5.1.1-windows-x64\5.1\python\bin\python.exe -S tools\render_tracking_preview.py --input outputs\green_field_tracking.json --output outputs\green_field_tracking_preview.svg`
- SVG exists and contains `<svg`.

Out of scope:
- Video frame extraction.
- Real tracking.
- Blender code changes.

Final report required:
- Files changed.
- Commands run.
- Validation result.
- Blockers.
